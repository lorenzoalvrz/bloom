# --- generated build output (regenerated by build_final.py) ---
bloom-3d-local.html

# --- render / scratch artifacts (recreated each session) ---
t_*.png
*_pv.png
*_sheet.png
crt_*.png
clean*_pv.png
xbox_*.png
wmp_*.png
test_*.html

# --- one-off render/verify scripts ---
r*.py
roff.py

# --- working copy of the embed (build reads embed_portal.json) ---
embed_new.json

# --- python / os ---
__pycache__/
*.pyc
.DS_Store

# NOTE: intentionally COMMITTED (do not ignore):
#   build_final.py        (generator)
#   embed_portal.json     (asset store / source of truth, ~4MB)
#   three.min.js          (vendored r128 — needed for local render-verify)
#   README.md
# Optional to commit for asset re-processing: src_xbox_keyed.png, vt323.ttf
